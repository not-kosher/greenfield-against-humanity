const enterLobby = (game, client) => {
  
};

const createRoom = (game, client, roomname) => {

};

const joinRoom = (game, client, roomname) => {

};

module.exports = {
  enterLobby,
  createRoom,
  joinRoom
};